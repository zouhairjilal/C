#include <stdio.h>

int main() {

    // constant = a fixed value that cannot be changed during the execution of the program

    const float PI = 3.14159;
    
    // PI = 420.69;

    printf("%f", PI);

    return 0;
}